version: 2.1
orbs:
  cypress: cypress-io/cypress@2.2.0

workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          requires:
            - cypress/install
          store-artifacts: true
          parallel: true
          record: true
          parallelism: 3
          group: UI Tests